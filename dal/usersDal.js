/**
 * find a user that : email equals the passed email or the username
 * equals the passed username
 * @param {*} param0 - {email, username}
 */
export async function findUserByEmailOrUsername({ email, username }) {}

/**
 * add user to the db.
 * @param {*} param0 - {email, username}
 */
export async function addUser({ email, username, password}) {}
